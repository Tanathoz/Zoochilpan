@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="panel panel-default">
                    <div class="panel-heading">Ejemplar {{ $ejemplar->marcaje }}</div>
                    <div class="panel-body">

                        <a href="{{ url('/Zoochilpan/ejemplar') }}" title="Back"><button class="btn btn-warning btn-xs"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/Zoochilpan/ejemplar/' . $ejemplar->marcaje . '/edit') }}" title="Edit Ejemplar"><button class="btn btn-primary btn-xs"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['Zoochilpan/ejemplar', $ejemplar->marcaje],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-xs',
                                    'title' => 'Delete Ejemplar',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table table-borderless">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $ejemplar->marcaje }}</td>
                                    </tr>
                                    <tr><th> FechaNacimiento </th><td> {{ $ejemplar->fechaNacimiento }} </td></tr><tr><th> FechaIngreso </th><td> {{ $ejemplar->fechaIngreso }} </td></tr><tr><th> Sexo </th><td> {{ $ejemplar->sexo }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
